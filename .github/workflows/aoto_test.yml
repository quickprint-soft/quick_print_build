name: GTest CI/CD Pipeline

on:
  push:
    branches: [ main, develop, master ]
  pull_request:
    branches: [ main, develop, master ]
  workflow_dispatch:  # 允许手动触发

jobs:
  build-and-test:
    runs-on: windows-latest
    
    steps:
    - name: 📥 检出代码
      uses: actions/checkout@v4
      with:
        submodules: recursive  # 如果有子模块
        
    - name: 🐍 设置 Python 环境
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        cache: 'pip'  # 缓存 pip 依赖
        
    - name: 🔧 配置 MSVC 编译器
      uses: ilammy/msvc-dev-cmd@v1
      with:
        arch: x64
        
    - name: 📦 创建构建目录
      run: |
        if (Test-Path build) { Remove-Item -Recurse -Force build }
        mkdir build
      shell: pwsh
        
    - name: ⚙️ 配置 CMake
      working-directory: build
      run: cmake .. -G "Visual Studio 17 2022" -A x64
      
    - name: 🔨 编译项目 (Release)
      working-directory: build
      run: cmake --build . --target gtest_run --config Release --parallel
        
    - name: 📂 准备测试报告目录
      run: |
        if (Test-Path test_reports) { Remove-Item -Recurse -Force test_reports }
        mkdir test_reports
      shell: pwsh
        
    - name: 🧪 运行 GTest 测试套件
      run: |
        $env:PATH += ";$PWD\lib64"
        .\lib64\gtest_run.exe --gtest_output=xml:test_reports/test_results.xml --gtest_print_time=1
        $testExitCode = $LASTEXITCODE
        echo "TEST_EXIT_CODE=$testExitCode" >> $env:GITHUB_ENV
        exit 0
      shell: pwsh
      continue-on-error: true
      
    - name: 📊 生成 HTML 测试报告
      if: always()
      run: |
        python generate_test_report.py -i test_reports/test_results.xml -o test_reports/test_report.html
      continue-on-error: true
      
    - name: 📝 生成测试摘要
      if: always()
      run: |
        if (Test-Path test_reports/test_results.xml) {
          $xml = [xml](Get-Content test_reports/test_results.xml)
          $testsuites = $xml.testsuites
          $total = $testsuites.tests
          $failures = $testsuites.failures
          $errors = $testsuites.errors
          $time = $testsuites.time
          $passed = [int]$total - [int]$failures - [int]$errors
          $passRate = if ([int]$total -gt 0) { [math]::Round(($passed / [int]$total) * 100, 2) } else { 0 }
          
          echo "## 🧪 GTest 测试结果" >> $env:GITHUB_STEP_SUMMARY
          echo "" >> $env:GITHUB_STEP_SUMMARY
          echo "| 指标 | 数值 |" >> $env:GITHUB_STEP_SUMMARY
          echo "|------|------|" >> $env:GITHUB_STEP_SUMMARY
          echo "| ✅ 通过 | $passed / $total |" >> $env:GITHUB_STEP_SUMMARY
          echo "| ❌ 失败 | $failures |" >> $env:GITHUB_STEP_SUMMARY
          echo "| ⚠️ 错误 | $errors |" >> $env:GITHUB_STEP_SUMMARY
          echo "| 📈 通过率 | $passRate% |" >> $env:GITHUB_STEP_SUMMARY
          echo "| ⏱️ 总耗时 | $time 秒 |" >> $env:GITHUB_STEP_SUMMARY
          echo "" >> $env:GITHUB_STEP_SUMMARY
          
          if ([int]$failures -gt 0 -or [int]$errors -gt 0) {
            echo "### ⚠️ 存在失败的测试用例" >> $env:GITHUB_STEP_SUMMARY
            echo "" >> $env:GITHUB_STEP_SUMMARY
            echo "请查看 [测试报告](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}) 了解详情。" >> $env:GITHUB_STEP_SUMMARY
          } else {
            echo "### ✅ 所有测试通过！" >> $env:GITHUB_STEP_SUMMARY
          }
        }
      shell: pwsh
      
    - name: 📤 上传测试报告 (Artifacts)
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: test-reports-${{ github.run_number }}
        path: |
          test_reports/test_results.xml
          test_reports/test_report.html
        retention-days: 30
        
    - name: 📊 发布测试结果到 PR
      uses: EnricoMi/publish-unit-test-result-action/windows@v2
      if: always() && github.event_name == 'pull_request'
      with:
        files: test_reports/test_results.xml
        check_name: 🧪 GTest 测试结果
        comment_title: 📊 GTest 测试报告
        comment_mode: always
        
    - name: ✅ 检查测试结果
      if: always()
      run: |
        if ($env:TEST_EXIT_CODE -ne "0") {
          Write-Host "❌ 测试失败！退出码: $env:TEST_EXIT_CODE"
          exit 1
        } else {
          Write-Host "✅ 所有测试通过！"
        }
      shell: pwsh
